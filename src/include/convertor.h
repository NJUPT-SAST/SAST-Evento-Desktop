#ifndef CONVERTOR_H
#define CONVERTOR_H

template<typename DTO, typename Entity>
class Convertor {
    //static Entity operator()(const DTO& src);
};

/*
template<>
class Convertor<DTO, Entity> {
    static Entity operator()(const DTO& src) {
        
    }
};
*/

#endif